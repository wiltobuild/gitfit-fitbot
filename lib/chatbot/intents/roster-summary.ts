import { createSupabaseServerClient } from "@/lib/supabase/server";
import { listMembersForStaff } from "@/lib/members/queries";
import type { Intent } from "@/lib/chatbot/types";
import { scoreEntity, scoreTriggerFamily } from "@/lib/chatbot/match-scoring";
function breakdown(values: Array<string | null | undefined>) { const counts = new Map<string, number>(); values.forEach((value) => { const key = value || "not set"; counts.set(key, (counts.get(key) ?? 0) + 1); }); return [...counts.entries()].map(([key, count]) => `${count} ${key}`).join(", "); }
export async function getRosterSummary() { const supabase = await createSupabaseServerClient(); const { data, error } = await listMembersForStaff(supabase); if (error) return { reply: "I couldn't retrieve the roster right now. Please try again shortly." }; const members = data ?? []; const attention = members.filter((member) => ["at_risk", "lapsed"].includes(member.lifecycle_status)).slice(0, 8); return { reply: `Roster summary: ${breakdown(members.map((member) => member.lifecycle_status))}. Membership tiers: ${breakdown(members.map((member) => member.membership_tier))}.`, card: { kind: "members" as const, title: "Members needing attention", members: (attention.length ? attention : members.slice(0, 8)).map((member) => ({ name: member.full_name || member.email, email: member.email, status: member.lifecycle_status, reason: `Membership tier: ${member.membership_tier ?? "not set"}` })) } }; }
export const rosterSummaryIntent: Intent = { id: "roster-summary", description: "Summarizes roster lifecycle and membership tiers.", roles: ["staff", "admin"], match: (message) => Number(/\b(roster summary|member summary|membership breakdown)\b/i.test(message)), handle: async () => getRosterSummary() };
