# Project Profile

_Generated by /bootstrap-project. Re-run bootstrap to refresh; do not hand-edit
the "Repository Facts" section — it should reflect a fresh inspection._

## Identity

- **What**: The GitFit Suite — a unified fitness-management product this
  repo owns end-to-end (Supabase auth/roles, schedule, appointments, member
  records, retention/outreach), with a deterministic-first chatbot as the
  suite's persistent, native intelligent interface. Supersedes the earlier
  "chatbot orchestrates teammates' separate products via a shared contract"
  model in [docs/build-doc.md](../build-doc.md) — see the 2026-08-18
  architecture-pivot entry in `decisions.md`. `docs/build-doc.md` is kept for
  brand tokens (palette/fonts/voice) and historical context, not as the
  current architecture doc.
- **Audience**: Capstone team (4 people, one product each) + course
  instructor/grader; end users are demo-day viewers exercising the chat UI.
- **Evaluation context**: Capstone project — graded, portfolio-worthy. Needs
  to demonstrate a working tool-calling loop against real (or at least
  realistic) teammate product endpoints, not just a scripted demo.
- **Success looks like**: A deployed, working chat UI that can hold a
  conversation, call at least one real teammate product's tool endpoint with
  a visible confirmation step for mutating actions, and gracefully degrade if
  a teammate's product is down.
- **Deadline / milestones**: Ship Friday, Aug 28, 2026.
- **Origin**: New work — brand-new repo, no prior code.

## Repository Facts (verified by inspection)

- Branch: `main`
- Commit: none yet (no commits made)
- Working tree: clean, empty (only `docs/` created so far)
- Languages/frameworks: TBD — recommended Node.js + TypeScript (backend),
  React via Vite or Next.js + Tailwind (frontend), per docs/build-doc.md
- Package manager: TBD (likely npm or pnpm — this workspace's other recent
  projects use pnpm)
- Test framework(s): TBD
- Build/lint/typecheck commands: TBD — not yet chosen
- Persistence: Postgres (or SQLite for the demo), conversation turns keyed
  by `user_id` — per docs/build-doc.md, not yet implemented
- Deployment target: TBD — candidates are Render, Fly.io, or Vercel
  (frontend) + Render (backend), per docs/build-doc.md
- Entry points: none yet

## Inferences (confirmed by user)

- None — repo is brand new.

## Unknowns

- MCP vs REST+OpenAPI for the tool manifest — team hasn't picked yet (open
  question in docs/build-doc.md).
- Exact teammate product APIs/manifests — not yet published by teammates.
- Shared JWT secret storage/rotation ownership — not yet decided.
- Final frontend framework choice (Vite vs Next.js) and package manager.
- Golden-path demo script — not yet defined.

## Constraints

- Data sensitivity: No special handling required per user (health/practice
  data sensitivity note in docs/build-doc.md is aspirational guidance, not a
  hard constraint for this project).
- Breaking-change tolerance: High pre-launch (new repo, no users yet);
  changes to the shared contract (tool manifest format, auth shape, error
  shape) affect teammates' products and need sign-off — see Approval gates.
- Other constraints:
  - Secrets (API keys, shared JWT secret) must never be committed — use
    gitignored `.env` files.

## Role ownership

- Product direction & tradeoffs: user
- Investigation, planning, delegation, review coordination, verification: Claude
- Implementation: Codex (`codex exec`)
- Overrides: none

## Approval gates

Beyond the global defaults in `~/.claude/CLAUDE.md` (architecture, schema,
auth, new dependencies, destructive actions, scope expansion):

- Any change to the shared contract — tool manifest format, JWT/auth claim
  shape, or error response shape — needs explicit user sign-off before
  implementation, since it's a breaking change for teammates' products, not
  just this repo.

## Documentation level

- **Standard** for the project overall.
- No elevated Regulated-equivalent code path identified.
